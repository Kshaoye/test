server:
  port: 9005
spring:
  application:
    name: springcloud-config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/Kshaoye/test.git
          search-paths: /src/main/resources/config
          username: kshaoye
          password: sn930908
